{
    "name": "document-uploader-lambda",
    "version": "1.0.0",
    "description": "Presigned URL generator + metadata registration — ingestion pipeline stage 1",
    "type": "module",
    "main": "src/handler.js",
    "scripts": {
      "test": "node --experimental-vm-modules node_modules/.bin/jest",
      "package": "zip -r function.zip src/ node_modules/ package.json"
    },
    "dependencies": {
      "@aws-sdk/client-dynamodb": "^3.600.0",
      "@aws-sdk/client-s3": "^3.600.0",
      "@aws-sdk/s3-request-presigner": "^3.600.0",
      "@aws-sdk/util-dynamodb": "^3.600.0"
    }
  }